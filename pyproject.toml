[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kami-sim"
version = "0.1.0"
description = "LLM-driven multi-agent town simulation"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["backend/kami_sim"]
